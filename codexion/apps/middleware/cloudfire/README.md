# Cloudfire Folder

This folder is reserved for cloud/edge deployment artifacts for the middleware, for example:
- edge runtime deployment manifests,
- secure tunnel config to condo LAN,
- sync worker definitions to forward local outbox events.

Suggested next files:
- `cloudfire/edge-runtime-config.yaml`
- `cloudfire/sync-worker-config.json`
