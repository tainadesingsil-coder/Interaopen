name: Cloudflare Pages (Legacy Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: echo "This legacy workflow is disabled. Use 'Cloudflare Pages (Codexion Dashboard)'."